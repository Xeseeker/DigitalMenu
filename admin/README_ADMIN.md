Admin app (Vite + React + Tailwind)

Development

1. Install dependencies: `npm install` in the `admin` folder.
2. Start dev server: `npm run dev`.

Set `VITE_API_BASE` in `.env` or `.env.local` to point to backend (default http://localhost:5000).
